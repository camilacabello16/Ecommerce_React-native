class UserInfo {
    constructor(userName, password, fullName, userId) {
        this.userName = userName;
        this.password = password;
        this.fullName = fullName;
        this.userId = userId;
    }
}

export default UserInfo;